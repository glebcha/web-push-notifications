export const API_URL = 'https://push.mirtesen.ru/api/push';
export const PREFIX = 'web_push_lib';
export const SW_PATH = `${PREFIX}_sw_path`;

export const APP_VERSION_PREFIX = `${PREFIX}_push_version`;
export const APP_ID_PREFIX = `${PREFIX}_push_id`;
export const APP_NAME_PREFIX = `${PREFIX}_push_name`;
export const APP_INSTALL_DATE = `${PREFIX}_push_install_date`;

export const FCM_TOKEN_PREFIX = `${PREFIX}_fcm_token`;

export const USER_ID_PREFIX = `${PREFIX}_user_id`;

export const NOTIFICATIONS_ACCEPTED_EVENT = `${PREFIX}_notifications_accepted`;
export const NOTIFICATIONS_DECLINED_EVENT = `${PREFIX}_notifications_declined`;

export const DEFAULT_SW_PATH = 'service-worker.js';

export const API_REGISTER = 'register';
export const API_REGISTER_SAFARI = 'register-safari';
export const API_UNREGISTER = 'unregister';
export const API_UNREGISTER_SAFARI = 'unregister-safari';

export const API_SAFARI_ENDPOINT = 'safari';
export const API_SAFARI_APPLICATION_ID = 'web-safari-push';

export const API_WEB_PUSH_ID = 'web.com.push';
